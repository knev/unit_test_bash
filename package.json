{
  "scripts": {
    "build": "antora --fetch antora-playbook.yml",
    "build:local": "antora --fetch antora-playbook-local.yml",
    "clean": "del build && echo Ok."
  },
  "devDependencies": {
    "@antora/cli": "3.1.2",
    "@antora/site-generator": "3.1.2",
    "del-cli": "^5.0.0"
  }
}
